# Secrets — HuggingFace token lives here, NEVER commit
.env
.env.*
!.env.example

# Python
.venv/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Claude Code skill metadata — only used when this lives under ~/.claude/skills/.
# Keep it out of the public repo so the project reads as a standalone tool.
AGENTS.md
SKILL.md


# Demo data — used during local development as a Claude Code skill,
# not part of the public-facing project.
demo/

# Generated artifacts (per-run output, don't commit)
*.transcript.json
*.whisper-cache.json
*.selections.json
*.final.mp4
*.final.mov
*.social.md

# Model caches (multi-GB; users download themselves)
.cache/
cache/
huggingface/
modelscope/

# Docker build artifacts
.docker/

# Runtime data
current_transcript.txt
selections.json
